
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // For simplicity initially, or can configure remote patterns if needed
  },
};

export default nextConfig;
